#Codigo aportado por el enunciado del ejercicio
def sumar(s1: int, s2: int) -> int:
    resultado = s1 + s2
    return resultado


def restar(r1: int, r2: int) -> int:
    resultado = r1 - r2
    return resultado

